import * as solid_js from 'solid-js';
import { JSX, ValidComponent, ComponentProps, ParentProps } from 'solid-js';
import { b as ButtonRootOptions, a as ButtonRootCommonProps, d as ButtonRootRenderProps } from './button-root-dcc87a1f.js';
import { PolymorphicProps } from './polymorphic/index.js';
import { a as DismissableLayerCommonProps, D as DismissableLayerRenderProps } from './dismissable-layer-0b7def90.js';
import { PointerDownOutsideEvent, FocusOutsideEvent, InteractOutsideEvent } from './primitives/create-interact-outside/index.js';
import { Portal } from 'solid-js/web';

interface DialogCloseButtonOptions extends ButtonRootOptions {
}
interface DialogCloseButtonCommonProps extends ButtonRootCommonProps {
    onClick: JSX.EventHandlerUnion<HTMLElement, MouseEvent>;
    "aria-label": string;
}
interface DialogCloseButtonRenderProps extends DialogCloseButtonCommonProps, ButtonRootRenderProps {
}
type DialogCloseButtonProps = DialogCloseButtonOptions & Partial<DialogCloseButtonCommonProps>;
/**
 * The button that closes the dialog.
 */
declare function DialogCloseButton<T extends ValidComponent = "button">(props: PolymorphicProps<T, DialogCloseButtonProps>): JSX.Element;

interface DialogContentOptions {
    /**
     * Event handler called when focus moves into the component after opening.
     * It can be prevented by calling `event.preventDefault`.
     */
    onOpenAutoFocus?: (event: Event) => void;
    /**
     * Event handler called when focus moves to the trigger after closing.
     * It can be prevented by calling `event.preventDefault`.
     */
    onCloseAutoFocus?: (event: Event) => void;
    /**
     * Event handler called when the escape key is down.
     * It can be prevented by calling `event.preventDefault`.
     */
    onEscapeKeyDown?: (event: KeyboardEvent) => void;
    /**
     * Event handler called when a pointer event occurs outside the bounds of the component.
     * It can be prevented by calling `event.preventDefault`.
     */
    onPointerDownOutside?: (event: PointerDownOutsideEvent) => void;
    /**
     * Event handler called when the focus moves outside the bounds of the component.
     * It can be prevented by calling `event.preventDefault`.
     */
    onFocusOutside?: (event: FocusOutsideEvent) => void;
    /**
     * Event handler called when an interaction (pointer or focus event) happens outside the bounds of the component.
     * It can be prevented by calling `event.preventDefault`.
     */
    onInteractOutside?: (event: InteractOutsideEvent) => void;
}
interface DialogContentCommonProps extends DismissableLayerCommonProps {
    id: string;
}
interface DialogContentRenderProps extends DialogContentCommonProps, DismissableLayerRenderProps {
    role: "dialog" | "alertdialog";
    tabIndex: -1;
}
type DialogContentProps = DialogContentOptions & Partial<DialogContentCommonProps>;
/**
 * Contains the content to be rendered when the dialog is open.
 */
declare function DialogContent<T extends ValidComponent = "div">(props: PolymorphicProps<T, DialogContentProps>): solid_js.JSX.Element;

interface DialogDescriptionOptions {
}
interface DialogDescriptionCommonProps {
    id: string;
}
interface DialogDescriptionRenderProps extends DialogDescriptionCommonProps {
}
type DialogDescriptionProps = DialogDescriptionOptions & Partial<DialogDescriptionCommonProps>;
/**
 * An optional accessible description to be announced when the dialog is open.
 */
declare function DialogDescription<T extends ValidComponent = "p">(props: PolymorphicProps<T, DialogDescriptionProps>): solid_js.JSX.Element;

interface DialogOverlayOptions {
}
interface DialogOverlayCommonProps {
    ref: HTMLElement | ((el: HTMLElement) => void);
    onPointerDown: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    /** The HTML styles attribute (object form only). */
    style: JSX.CSSProperties;
}
interface DialogOverlayRenderProps extends DialogOverlayCommonProps {
    "data-expanded": string | undefined;
    "data-closed": string | undefined;
}
type DialogOverlayProps = DialogOverlayOptions & Partial<DialogOverlayCommonProps>;
/**
 * A layer that covers the inert portion of the view when the dialog is open.
 */
declare function DialogOverlay<T extends ValidComponent = "div">(props: PolymorphicProps<T, DialogOverlayProps>): JSX.Element;

interface DialogPortalProps extends ComponentProps<typeof Portal> {
}
/**
 * Portals its children into the `body` when the dialog is open.
 */
declare function DialogPortal(props: DialogPortalProps): solid_js.JSX.Element;

declare const DIALOG_INTL_TRANSLATIONS: {
    dismiss: string;
};
type DialogIntlTranslations = typeof DIALOG_INTL_TRANSLATIONS;

interface DialogRootOptions {
    /** The localized strings of the component. */
    translations?: DialogIntlTranslations;
    /** The controlled open state of the dialog. */
    open?: boolean;
    /**
     * The default open state when initially rendered.
     * Useful when you do not need to control the open state.
     */
    defaultOpen?: boolean;
    /** Event handler called when the open state of the dialog changes. */
    onOpenChange?: (isOpen: boolean) => void;
    /**
     * A unique identifier for the component.
     * The id is used to generate id attributes for nested components.
     * If no id prop is provided, a generated id will be used.
     */
    id?: string;
    /**
     * Whether the dialog should be the only visible content for screen readers.
     * When set to `true`:
     * - interaction with outside elements will be disabled.
     * - scroll will be locked.
     * - focus will be locked inside the dialog content.
     * - elements outside the dialog content will not be visible for screen readers.
     */
    modal?: boolean;
    /** Whether the scroll should be locked even if the dialog is not modal. */
    preventScroll?: boolean;
    /**
     * Used to force mounting the dialog (portal, overlay and content) when more control is needed.
     * Useful when controlling animation with SolidJS animation libraries.
     */
    forceMount?: boolean;
}
interface DialogRootProps extends ParentProps<DialogRootOptions> {
}
/**
 * A dialog is a window overlaid on either the primary window or another dialog window.
 */
declare function DialogRoot(props: DialogRootProps): solid_js.JSX.Element;

interface DialogTitleOptions {
}
interface DialogTitleCommonProps {
    id: string;
}
interface DialogTitleRenderProps extends DialogTitleCommonProps {
}
type DialogTitleProps = DialogTitleOptions & Partial<DialogTitleCommonProps>;
/**
 * An accessible title to be announced when the dialog is open.
 */
declare function DialogTitle<T extends ValidComponent = "h2">(props: PolymorphicProps<T, DialogTitleProps>): solid_js.JSX.Element;

interface DialogTriggerOptions {
}
interface DialogTriggerCommonProps extends ButtonRootCommonProps {
    onClick: JSX.EventHandlerUnion<HTMLElement, MouseEvent>;
}
interface DialogTriggerRenderProps extends DialogTriggerCommonProps, ButtonRootRenderProps {
    "aria-haspopup": "dialog";
    "aria-expanded": boolean;
    "aria-controls": string | undefined;
    "data-expanded": string | undefined;
    "data-closed": string | undefined;
}
type DialogTriggerProps = DialogTriggerOptions & Partial<DialogTriggerCommonProps>;
/**
 * The button that opens the dialog.
 */
declare function DialogTrigger<T extends ValidComponent = "button">(props: PolymorphicProps<T, DialogTriggerProps>): JSX.Element;

export { DialogTitleCommonProps as A, DialogTitleOptions as B, DialogTitleProps as C, DialogRoot as D, DialogTitleRenderProps as E, DialogTriggerCommonProps as F, DialogTriggerOptions as G, DialogTriggerProps as H, DialogTriggerRenderProps as I, DialogCloseButton as a, DialogContent as b, DialogDescription as c, DialogOverlay as d, DialogPortal as e, DialogTitle as f, DialogTrigger as g, DialogCloseButtonCommonProps as h, DialogCloseButtonOptions as i, DialogCloseButtonProps as j, DialogCloseButtonRenderProps as k, DialogContentCommonProps as l, DialogContentOptions as m, DialogContentProps as n, DialogContentRenderProps as o, DialogDescriptionCommonProps as p, DialogDescriptionOptions as q, DialogDescriptionProps as r, DialogDescriptionRenderProps as s, DialogOverlayCommonProps as t, DialogOverlayOptions as u, DialogOverlayProps as v, DialogOverlayRenderProps as w, DialogPortalProps as x, DialogRootOptions as y, DialogRootProps as z };
