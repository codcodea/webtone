import * as solid_js from 'solid-js';
import { JSX, ValidComponent, ComponentProps, ParentProps } from 'solid-js';
import { PolymorphicProps } from './polymorphic/index.js';
import { Portal } from 'solid-js/web';
import { g as PopperRootOptions } from './popper-root-07fed9c2.js';
import { D as DismissableLayerRenderProps } from './dismissable-layer-0b7def90.js';
import { PointerDownOutsideEvent, FocusOutsideEvent, InteractOutsideEvent } from './primitives/create-interact-outside/index.js';

interface MenuOptions extends Omit<PopperRootOptions, "anchorRef" | "contentRef" | "onCurrentPlacementChange"> {
    /** The controlled open state of the menu. */
    open?: boolean;
    /**
     * The default open state when initially rendered.
     * Useful when you do not need to control the open state.
     */
    defaultOpen?: boolean;
    /** Event handler called when the open state of the menu changes. */
    onOpenChange?: (isOpen: boolean) => void;
}

interface MenuItemDataSet {
    "data-indeterminate": string | undefined;
    "data-checked": string | undefined;
    "data-disabled": string | undefined;
    "data-highlighted": string | undefined;
}

interface MenuItemBaseOptions {
    /**
     * Optional text used for typeahead purposes.
     * By default, the typeahead behavior will use the .textContent of the Menu.ItemLabel part
     * if provided, or fallback to the .textContent of the Menu.Item.
     * Use this when the content is complex, or you have non-textual content inside.
     */
    textValue?: string;
    /** Whether the menu item is disabled. */
    disabled?: boolean;
    /** Whether the menu item is checked (item radio or item checkbox). */
    checked?: boolean;
    /**
     * When using menu item checkbox, whether the checked state is in an indeterminate mode.
     * Indeterminism is presentational only.
     * The indeterminate visual representation remains regardless of user interaction.
     */
    indeterminate?: boolean;
    /** Whether the menu should close when the menu item is activated/selected. */
    closeOnSelect?: boolean;
    /** Event handler called when the user selects an item (via mouse or keyboard). */
    onSelect?: () => void;
}
interface MenuItemBaseCommonProps {
    id: string;
    ref: HTMLElement | ((el: HTMLElement) => void);
    onPointerMove: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onPointerLeave: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onPointerDown: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onPointerUp: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onClick: JSX.EventHandlerUnion<HTMLElement, MouseEvent>;
    onKeyDown: JSX.EventHandlerUnion<HTMLElement, KeyboardEvent>;
    onMouseDown: JSX.EventHandlerUnion<HTMLElement, MouseEvent>;
    onFocus: JSX.EventHandlerUnion<HTMLElement, FocusEvent>;
}
interface MenuItemBaseRenderProps extends MenuItemBaseCommonProps, MenuItemDataSet {
    tabIndex: number | undefined;
    "aria-checked": boolean | "mixed" | undefined;
    "aria-disabled": boolean | undefined;
    "aria-labelledby": string | undefined;
    "aria-describedby": string | undefined;
    "data-key": string | undefined;
}

interface MenuCheckboxItemOptions extends Omit<MenuItemBaseOptions, "checked"> {
    /** The controlled checked state of the menu item checkbox. */
    checked?: boolean;
    /**
     * The default checked state when initially rendered.
     * Useful when you do not need to control the checked state.
     */
    defaultChecked?: boolean;
    /** Event handler called when the checked state of the menu item checkbox changes. */
    onChange?: (isChecked: boolean) => void;
}
interface MenuCheckboxItemCommonProps extends MenuItemBaseCommonProps {
}
interface MenuCheckboxItemRenderProps extends MenuCheckboxItemCommonProps, MenuItemBaseRenderProps {
    role: "menuitemcheckbox";
}
type MenuCheckboxItemProps = MenuCheckboxItemOptions & Partial<MenuCheckboxItemCommonProps>;
/**
 * An item that can be controlled and rendered like a checkbox.
 */
declare function MenuCheckboxItem<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuCheckboxItemProps>): solid_js.JSX.Element;

interface MenuDataSet {
    "data-expanded": string | undefined;
    "data-closed": string | undefined;
}

interface MenuContentBaseOptions {
    /**
     * Event handler called when focus moves into the component after opening.
     * It can be prevented by calling `event.preventDefault`.
     */
    onOpenAutoFocus?: (event: Event) => void;
    /**
     * Event handler called when focus moves to the trigger after closing.
     * It can be prevented by calling `event.preventDefault`.
     */
    onCloseAutoFocus?: (event: Event) => void;
    /**
     * Event handler called when the escape key is down.
     * It can be prevented by calling `event.preventDefault`.
     */
    onEscapeKeyDown?: (event: KeyboardEvent) => void;
    /**
     * Event handler called when a pointer event occurs outside the bounds of the component.
     * It can be prevented by calling `event.preventDefault`.
     */
    onPointerDownOutside?: (event: PointerDownOutsideEvent) => void;
    /**
     * Event handler called when the focus moves outside the bounds of the component.
     * It can be prevented by calling `event.preventDefault`.
     */
    onFocusOutside?: (event: FocusOutsideEvent) => void;
    /**
     * Event handler called when an interaction (pointer or focus event) happens outside the bounds of the component.
     * It can be prevented by calling `event.preventDefault`.
     */
    onInteractOutside?: (event: InteractOutsideEvent) => void;
}
interface MenuContentBaseCommonProps {
    id: string;
    ref: HTMLElement | ((el: HTMLElement) => void);
    onPointerEnter: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onPointerMove: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onKeyDown: JSX.EventHandlerUnion<HTMLElement, KeyboardEvent>;
    onMouseDown: JSX.EventHandlerUnion<HTMLElement, MouseEvent>;
    onFocusIn: JSX.EventHandlerUnion<HTMLElement, FocusEvent>;
    onFocusOut: JSX.EventHandlerUnion<HTMLElement, FocusEvent>;
    /** The HTML styles attribute (object form only). */
    style?: JSX.CSSProperties;
}
interface MenuContentBaseRenderProps extends MenuContentBaseCommonProps, DismissableLayerRenderProps, MenuDataSet {
    role: "menu";
    tabIndex: number | undefined;
    "aria-labelledby": string | undefined;
}

interface MenuContentOptions extends MenuContentBaseOptions {
}
interface MenuContentCommonProps extends MenuContentBaseCommonProps {
}
interface MenuContentRenderProps extends MenuContentCommonProps, MenuContentBaseRenderProps {
}
type MenuContentProps = MenuContentOptions & Partial<MenuContentCommonProps>;
declare function MenuContent<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuContentProps>): solid_js.JSX.Element;

interface MenuGroupOptions {
}
interface MenuGroupCommonProps {
    id: string;
}
interface MenuGroupRenderProps extends MenuGroupCommonProps {
    role: "group";
    "aria-labelledby": string | undefined;
}
type MenuGroupProps = MenuGroupOptions & Partial<MenuGroupCommonProps>;
/**
 * A container used to group multiple `Menu.Item`s.
 */
declare function MenuGroup<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuGroupProps>): solid_js.JSX.Element;

interface MenuGroupLabelOptions {
}
interface MenuGroupLabelCommonProps {
    id: string;
}
interface MenuGroupLabelRenderProps extends MenuGroupLabelCommonProps {
    "aria-hidden": "true";
}
type MenuGroupLabelProps = MenuGroupLabelOptions & Partial<MenuGroupLabelCommonProps>;
/**
 * A component used to render the label of a `Menu.Group`.
 * It won't be focusable using arrow keys.
 */
declare function MenuGroupLabel<T extends ValidComponent = "span">(props: PolymorphicProps<T, MenuGroupLabelProps>): solid_js.JSX.Element;

interface MenuIconOptions {
}
interface MenuIconCommonProps {
    children: JSX.Element;
}
interface MenuIconRenderProps extends MenuIconCommonProps, MenuDataSet {
    "aria-hidden": "true";
}
type MenuIconProps = MenuIconOptions & Partial<MenuIconCommonProps>;
/**
 * A small icon often displayed inside the menu trigger as a visual affordance for the fact it can be open.
 * It renders a `▼` by default, but you can use your own icon by providing a `children`.
 */
declare function MenuIcon<T extends ValidComponent = "span">(props: PolymorphicProps<T, MenuIconProps>): JSX.Element;

interface MenuItemOptions extends Omit<MenuItemBaseOptions, "checked" | "indeterminate"> {
}
interface MenuItemCommonProps extends MenuItemBaseCommonProps {
}
interface MenuItemRenderProps extends MenuItemCommonProps, MenuItemBaseRenderProps {
    role: "menuitem";
}
type MenuItemProps = MenuItemOptions & Partial<MenuItemCommonProps>;
/**
 * An item of the menu.
 */
declare function MenuItem<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuItemProps>): solid_js.JSX.Element;

interface MenuItemDescriptionOptions {
}
interface MenuItemDescriptionCommonProps {
    id: string;
}
interface MenuItemDescriptionRenderProps extends MenuItemDescriptionCommonProps, MenuItemDataSet {
}
type MenuItemDescriptionProps = MenuItemDescriptionOptions & Partial<MenuItemDescriptionCommonProps>;
/**
 * An optional accessible description to be announced for the menu item.
 * Useful for menu items that have more complex content (e.g. icons, multiple lines of text, etc.)
 */
declare function MenuItemDescription<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuItemDescriptionProps>): solid_js.JSX.Element;

interface MenuItemIndicatorOptions {
    /**
     * Used to force mounting when more control is needed.
     * Useful when controlling animation with SolidJS animation libraries.
     */
    forceMount?: boolean;
}
interface MenuItemIndicatorCommonProps {
    id: string;
}
interface MenuItemIndicatorRenderProps extends MenuItemIndicatorCommonProps, MenuItemDataSet {
}
type MenuItemIndicatorProps = MenuItemIndicatorOptions & Partial<MenuItemIndicatorCommonProps>;
/**
 * The visual indicator rendered when the parent menu `CheckboxItem` or `RadioItem` is checked.
 * You can style this element directly, or you can use it as a wrapper to put an icon into, or both.
 */
declare function MenuItemIndicator<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuItemIndicatorProps>): solid_js.JSX.Element;

interface MenuItemLabelOptions {
}
interface MenuItemLabelCommonProps {
    id: string;
    ref: HTMLElement | ((el: HTMLElement) => void);
}
interface MenuItemLabelRenderProps extends MenuItemLabelCommonProps, MenuItemDataSet {
}
type MenuItemLabelProps = MenuItemLabelOptions & Partial<MenuItemLabelCommonProps>;
/**
 * An accessible label to be announced for the menu item.
 * Useful for menu items that have more complex content (e.g. icons, multiple lines of text, etc.)
 */
declare function MenuItemLabel<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuItemLabelProps>): solid_js.JSX.Element;

interface MenuPortalProps extends ComponentProps<typeof Portal> {
}
/**
 * Portals its children into the `body` when the menu is open.
 */
declare function MenuPortal(props: MenuPortalProps): solid_js.JSX.Element;

interface MenuRadioGroupOptions {
    /** The controlled value of the item radio to check. */
    value?: string;
    /**
     * The value of the item radio that should be checked when initially rendered.
     * Useful when you do not need to control the state of the menu radio group.
     */
    defaultValue?: string;
    /** Event handler called when the value changes. */
    onChange?: (value: string) => void;
    /** Whether the menu radio group is disabled. */
    disabled?: boolean;
}
interface MenuRadioGroupCommonProps extends MenuGroupCommonProps {
    id: string;
}
interface MenuRadioGroupRenderProps extends MenuRadioGroupCommonProps, MenuGroupRenderProps {
}
type MenuRadioGroupProps = MenuRadioGroupOptions & Partial<MenuRadioGroupCommonProps>;
/**
 * A container used to group multiple `Menu.RadioItem`s and manage the selection.
 */
declare function MenuRadioGroup<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuRadioGroupProps>): solid_js.JSX.Element;

interface MenuRadioItemOptions extends Omit<MenuItemBaseOptions, "checked" | "indeterminate"> {
    /** The value of the menu item radio. */
    value: string;
}
interface MenuRadioItemCommonProps extends MenuItemBaseCommonProps {
}
interface MenuRadioItemRenderProps extends MenuRadioItemCommonProps, MenuItemBaseRenderProps {
    role: "menuitemradio";
}
type MenuRadioItemProps = MenuRadioItemOptions & Partial<MenuRadioItemCommonProps>;
/**
 * An item that can be controlled and rendered like a radio.
 */
declare function MenuRadioItem<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuRadioItemProps>): solid_js.JSX.Element;

interface MenuRootOptions extends MenuOptions {
    /**
     * A unique identifier for the component.
     * The id is used to generate id attributes for nested components.
     * If no id prop is provided, a generated id will be used.
     */
    id?: string;
    /**
     * Whether the menu should be the only visible content for screen readers.
     * When set to `true`:
     * - interaction with outside elements will be disabled.
     * - scroll will be locked.
     * - focus will be locked inside the menu content.
     * - elements outside the menu content will not be visible for screen readers.
     */
    modal?: boolean;
    /** Whether the scroll should be locked even if the menu is not modal. */
    preventScroll?: boolean;
    /**
     * Used to force mounting the menu (portal, positioner and content) when more control is needed.
     * Useful when controlling animation with SolidJS animation libraries.
     */
    forceMount?: boolean;
    /**
     * A unique value that associates the item with an active value
     * when the navigation menu is controlled.
     * This prop is managed automatically when uncontrolled.
     * Only used inside a Menubar.
     */
    value?: string;
}

interface MenuSubOptions extends Omit<MenuOptions, "placement" | "flip" | "sameWidth"> {
}
interface MenuSubProps extends ParentProps<MenuSubOptions> {
}
/**
 * Contains all the parts of a submenu.
 */
declare function MenuSub(props: MenuSubProps): solid_js.JSX.Element;

interface MenuSubContentOptions extends Omit<MenuContentBaseOptions, "onOpenAutoFocus" | "onCloseAutoFocus"> {
}
interface MenuSubContentCommonProps extends MenuContentBaseCommonProps {
    onKeyDown: JSX.EventHandlerUnion<HTMLElement, KeyboardEvent>;
}
interface MenuSubContentRenderProps extends MenuSubContentCommonProps, MenuContentBaseRenderProps {
}
type MenuSubContentProps = MenuSubContentOptions & Partial<MenuSubContentCommonProps>;
/**
 * The component that pops out when a submenu is open.
 */
declare function MenuSubContent<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuSubContentProps>): JSX.Element;

interface MenuSubTriggerOptions {
    /**
     * Optional text used for typeahead purposes.
     * By default, the typeahead behavior will use the .textContent of the Menu.SubTrigger.
     * Use this when the content is complex, or you have non-textual content inside.
     */
    textValue?: string;
    /** Whether the sub menu trigger is disabled. */
    disabled?: boolean;
}
interface MenuSubTriggerCommonProps {
    id: string;
    ref: HTMLElement | ((el: HTMLElement) => void);
    onPointerMove: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onPointerLeave: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onPointerDown: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onPointerUp: JSX.EventHandlerUnion<HTMLElement, PointerEvent>;
    onClick: JSX.EventHandlerUnion<HTMLElement, MouseEvent>;
    onKeyDown: JSX.EventHandlerUnion<HTMLElement, KeyboardEvent>;
    onMouseDown: JSX.EventHandlerUnion<HTMLElement, MouseEvent>;
    onFocus: JSX.EventHandlerUnion<HTMLElement, FocusEvent>;
}
interface MenuSubTriggerRenderProps extends MenuSubTriggerCommonProps, MenuDataSet {
    role: "menuitem";
    tabIndex: number | undefined;
    "aria-haspopup": "true";
    "aria-expanded": boolean;
    "aria-controls": string | undefined;
    "aria-disabled": boolean | undefined;
    "data-key": string | undefined;
    "data-highlighted": "" | undefined;
    "data-disabled": "" | undefined;
}
type MenuSubTriggerProps = MenuSubTriggerOptions & Partial<MenuSubTriggerCommonProps>;
/**
 * An item that opens a submenu.
 */
declare function MenuSubTrigger<T extends ValidComponent = "div">(props: PolymorphicProps<T, MenuSubTriggerProps>): JSX.Element;

export { MenuRadioGroupOptions as $, MenuGroupLabelRenderProps as A, MenuGroupOptions as B, MenuGroupProps as C, MenuGroupRenderProps as D, MenuIconCommonProps as E, MenuIconOptions as F, MenuIconProps as G, MenuIconRenderProps as H, MenuItemCommonProps as I, MenuItemDescriptionCommonProps as J, MenuItemDescriptionOptions as K, MenuItemDescriptionProps as L, MenuDataSet as M, MenuItemDescriptionRenderProps as N, MenuItemIndicatorCommonProps as O, MenuItemIndicatorOptions as P, MenuItemIndicatorProps as Q, MenuItemIndicatorRenderProps as R, MenuItemLabelCommonProps as S, MenuItemLabelOptions as T, MenuItemLabelProps as U, MenuItemLabelRenderProps as V, MenuItemOptions as W, MenuItemProps as X, MenuItemRenderProps as Y, MenuPortalProps as Z, MenuRadioGroupCommonProps as _, MenuContentOptions as a, MenuRadioGroupProps as a0, MenuRadioGroupRenderProps as a1, MenuRadioItemCommonProps as a2, MenuRadioItemOptions as a3, MenuRadioItemRenderProps as a4, MenuRadioItemProps as a5, MenuSubContentCommonProps as a6, MenuSubContentOptions as a7, MenuSubContentProps as a8, MenuSubContentRenderProps as a9, MenuSubOptions as aa, MenuSubProps as ab, MenuSubTriggerCommonProps as ac, MenuSubTriggerOptions as ad, MenuSubTriggerProps as ae, MenuSubTriggerRenderProps as af, MenuContent as ag, MenuContentProps as ah, MenuContentCommonProps as b, MenuContentRenderProps as c, MenuRootOptions as d, MenuCheckboxItem as e, MenuGroup as f, MenuGroupLabel as g, MenuIcon as h, MenuItem as i, MenuItemDescription as j, MenuItemIndicator as k, MenuItemLabel as l, MenuPortal as m, MenuRadioGroup as n, MenuRadioItem as o, MenuSub as p, MenuSubContent as q, MenuSubTrigger as r, MenuCheckboxItemCommonProps as s, MenuCheckboxItemOptions as t, MenuCheckboxItemProps as u, MenuCheckboxItemRenderProps as v, MenuGroupCommonProps as w, MenuGroupLabelCommonProps as x, MenuGroupLabelOptions as y, MenuGroupLabelProps as z };
