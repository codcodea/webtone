import { JSX, ValidComponent } from 'solid-js';
import { D as DynamicProps } from '../types-ZqFfq4Im.js';
import { FloatingState } from '../create/floating.js';
import '../types-CfOU1RES.js';
import '@floating-ui/dom';

declare const DEFAULT_FLOATING_ARROW_ELEMENT = "div";
type FloatingArrowCorvuProps = {
    floatingState: FloatingState;
    /**
     * Size of the arrow in px.
     * @defaultValue 16
     */
    size?: number;
};
type FloatingArrowSharedElementProps = {
    style: JSX.CSSProperties;
    children: JSX.Element;
};
type FloatingArrowElementProps = FloatingArrowSharedElementProps;
type FloatingArrowProps = FloatingArrowCorvuProps & Partial<FloatingArrowSharedElementProps>;
declare const FloatingArrow: <T extends ValidComponent = "div">(props: DynamicProps<T, FloatingArrowProps, FloatingArrowElementProps>) => JSX.Element;

export { DEFAULT_FLOATING_ARROW_ELEMENT, type FloatingArrowCorvuProps, type FloatingArrowElementProps, type FloatingArrowProps, type FloatingArrowSharedElementProps, FloatingArrow as default };
